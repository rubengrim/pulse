[package]
name = "pulse"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 2

[dependencies]
# bevy = {version = "0.12.0", features = ["dynamic_linking"] }
bevy = {version = "0.12.0" }
bevy_flycam = "0.12.0"
bytemuck = "1.14.0"
